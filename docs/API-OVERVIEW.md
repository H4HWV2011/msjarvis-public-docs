# Public API Overview

This document summarizes the publicly documented endpoints exposed by the Ms. Jarvis system, grouped by function (gateway, consciousness, GIS, blockchain, background services).

Each endpoint is specified at a high level (method, path, purpose) and refers to the canonical OpenAPI specification in `api_specs/` for full parameter and schema details.
